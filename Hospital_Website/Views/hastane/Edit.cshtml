@model Hospital_Website.Models.hastane

@{
    ViewData["Title"] = "Edit";
}

<h1>@_localization.Getkey("Edit").Value</h1>

<h4>@_localization.Getkey("Hastane").Value</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Name" class="control-label">@_localization.Getkey("Hastane").Value :</label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ilceID" class="control-label">@_localization.Getkey("Ilce").Value :</label>
                <select asp-for="ilceID" class="form-control" asp-items="ViewBag.ilceID"></select>
                <span asp-validation-for="ilceID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="@_localization.Getkey("Save").Value" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">@_localization.Getkey("List").Value</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
